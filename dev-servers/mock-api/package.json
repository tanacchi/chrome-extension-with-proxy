{
  "name": "mock-api-server",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "vitest run --reporter=verbose",
    "test:dev": "vitest --watch",
    "test:watch": "vitest --watch",
    "test:postman": "newman run postman-collection.json --environment postman-environment.json --reporters cli,json --reporter-json-export test-results.json",
    "test:postman:local": "newman run postman-collection.json --global-var baseUrl=http://localhost:3001 --reporters cli,htmlextra --reporter-htmlextra-export postman-test-report.html",
    "test:e2e": "bash test-with-server.sh",
    "test:all": "npm run test && npm run test:e2e"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/supertest": "^6.0.3",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1",
    "supertest": "^7.1.1",
    "vitest": "^3.2.3"
  }
}
